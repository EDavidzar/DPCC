/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/GUIForms/JPanel.java to edit this template
 */
package ccpd.es.p8panels;

import ccpd.es.p8dbmanager.FDBManExtra;
import static beleris.es.finalinformationmanager.MainErrorManager.AcceptInformation;
import static beleris.es.finalinformationmanager.MainErrorManager.mconfyes;
import beleris.es.finalprimaryclasses.CLAllObjectList;
import beleris.es.finalprimaryclasses.CL_DBCV;
import static ccpd.es.p8config.P8Config.PConfig;
import java.util.List;
import java.util.ListIterator;
import javax.swing.table.DefaultTableModel;

/**
 *
 * @author Emilio David Diaus López 2008-2025
 */
public class Ta_TreatActivitiesRegister extends javax.swing.JPanel {

    private static final long serialVersionUID = -3876010743159941957L;

    /**
     * Creates new form Ta_TreatActivitiesRegister
     */
    public Ta_TreatActivitiesRegister() {
        initComponents();
    }

    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        jScrollPane1 = new javax.swing.JScrollPane();
        JPTable = new javax.swing.JTable();

        JPTable.setModel(new javax.swing.table.DefaultTableModel(
            new Object [][] {
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null}
            },
            new String [] {
                "Title 1", "Title 2", "Title 3", "Title 4"
            }
        ));
        jScrollPane1.setViewportView(JPTable);

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(this);
        this.setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGroup(layout.createSequentialGroup()
                .addContainerGap()
                .addComponent(jScrollPane1, javax.swing.GroupLayout.PREFERRED_SIZE, 375, javax.swing.GroupLayout.PREFERRED_SIZE)
                .addContainerGap(19, Short.MAX_VALUE))
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGroup(layout.createSequentialGroup()
                .addContainerGap()
                .addComponent(jScrollPane1, javax.swing.GroupLayout.PREFERRED_SIZE, 275, javax.swing.GroupLayout.PREFERRED_SIZE)
                .addContainerGap(19, Short.MAX_VALUE))
        );
    }// </editor-fold>//GEN-END:initComponents

    // Variables declaration - do not modify//GEN-BEGIN:variables
    private javax.swing.JTable JPTable;
    private javax.swing.JScrollPane jScrollPane1;
    // End of variables declaration//GEN-END:variables

    FDBManExtra DBMSAP;
    List<String> DEST;

    private void FillPanel() {
        DefaultTableModel model = new DefaultTableModel();
        model.addColumn(" idregistro ");
        model.addColumn(" Clente ");
        model.addColumn(" Empresa ");
        model.addColumn(" Actividad ");
        model.addColumn(" Fecha ");
        model.addColumn(" Notas ");
        model.setRowCount(0);
        int index = 1;
        if (!DEST.isEmpty()) {
            ListIterator<String> dli = DEST.listIterator();
            while (dli.hasNext()) {
                String TheNext = " " +dli.next() + " ";
                var Number = String.format("%03d", index);
                model.addRow(new Object[]{Number, TheNext});
                index++;
            }
            JPTable.setModel(model);
        }
        AddEmptyRow();

    }

    private void LoadList() {
        //SELECT * FROM ccpd_lists.list_cat_desttransint;
        PConfig.setDBName("ccpd_treat_activity_register");
        DBMSAP = new FDBManExtra(PConfig.getSQLURL(), PConfig.getAdminDBPassword(), PConfig.getDBName(), PConfig.getAdminDBName(), FDBManExtra.DB_MYSQL_Selected);
        DEST = DBMSAP.LoadList("treatactivityreg","idtreatactivityreg");

    }

    private void UpdateDBWithTableData() {
        int ntf = AcceptInformation("¿Está seguro de que quiere actualizar los datos?");
        if (ntf == mconfyes) {
            UpdateDBData();
        }
    }

    void AddEmptyRow() {
        int index = JPTable.getModel().getRowCount() + 1;
        DefaultTableModel model = (DefaultTableModel) JPTable.getModel();
        var Number = String.format("%03d", index);
        model.addRow(new Object[]{Number, ""});
    }

    private void InsertRow() {
        int index = JPTable.getModel().getRowCount() - 1;
        String idtreatactivity = (String) JPTable.getModel().getValueAt(index, 0);
        String sidclient = (String) JPTable.getModel().getValueAt(index, 1);
        String sidenterprise = (String) JPTable.getModel().getValueAt(index, 2);
        String sidactivity = (String) JPTable.getModel().getValueAt(index, 3);
        String sdate = (String) JPTable.getModel().getValueAt(index, 4);
        String snotes = (String) JPTable.getModel().getValueAt(index, 5);

        //CL_DBCV CamposPanel=new CL_DBCV();
        CLAllObjectList<CL_DBCV> List_CamposPanel = new CLAllObjectList<>();
        List_CamposPanel.add(new CL_DBCV(idtreatactivity, idtreatactivity));
        List_CamposPanel.add(new CL_DBCV(sidclient, idtreatactivity));
        List_CamposPanel.add(new CL_DBCV(sidenterprise, idtreatactivity));
        List_CamposPanel.add(new CL_DBCV(sidactivity, idtreatactivity));
        List_CamposPanel.add(new CL_DBCV(sdate, idtreatactivity));
        List_CamposPanel.add(new CL_DBCV(snotes, idtreatactivity));

        DBMSAP.InsertDatainDB("treatactivityreg", List_CamposPanel);
        AddEmptyRow();
    }

    private void DeleteRow() {
        int ntf = AcceptInformation("¿Está seguro de que quiere borrar la fila seleccionada?");
        if (ntf == mconfyes) {
            int index = JPTable.getSelectedRow();
            ((DefaultTableModel) JPTable.getModel()).removeRow(index);
        }
    }

    private void UpdateDBData() {
        int totalindex = JPTable.getModel().getRowCount();
        int index = 0;
        CLAllObjectList<CL_DBCV> List_CamposPanel = new CLAllObjectList<>();
        while (index < totalindex) {
            String sidtreatactivity = (String) JPTable.getModel().getValueAt(index, 0);
            String sidclient = (String) JPTable.getModel().getValueAt(index, 1);
            String sidenterprise = (String) JPTable.getModel().getValueAt(index, 2);
            String sidactivity = (String) JPTable.getModel().getValueAt(index, 3);
            String sdate = (String) JPTable.getModel().getValueAt(index, 4);
            String snotes = (String) JPTable.getModel().getValueAt(index, 5);

            if (!"".equals(sidclient)) {
                List_CamposPanel.add(new CL_DBCV(sidclient, sidtreatactivity));
            }
            if (!"".equals(sidenterprise)) {
                List_CamposPanel.add(new CL_DBCV(sidenterprise, sidtreatactivity));
            }
            if (!"".equals(sidactivity)) {
                List_CamposPanel.add(new CL_DBCV(sidactivity, sidtreatactivity));
            }
            if (!"".equals(sdate)) {
                List_CamposPanel.add(new CL_DBCV(sdate, sidtreatactivity));
            }
            if (!"".equals(snotes)) {
                List_CamposPanel.add(new CL_DBCV(snotes, sidtreatactivity));
            }

            index++;
        }
        DBMSAP.DeleteDataTable("treatactivityreg");
        DBMSAP.InsertDatainDB("treatactivityreg", List_CamposPanel);
    }
}
