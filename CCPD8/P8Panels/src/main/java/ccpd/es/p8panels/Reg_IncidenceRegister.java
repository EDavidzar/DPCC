/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/GUIForms/JPanel.java to edit this template
 */
package ccpd.es.p8panels;

import ccpd.es.p8dbmanager.FDBManExtra;
import static beleris.es.finalinformationmanager.MainErrorManager.AcceptInformation;
import static beleris.es.finalinformationmanager.MainErrorManager.mconfyes;
import beleris.es.finalprimaryclasses.CLAllObjectList;
import beleris.es.finalprimaryclasses.CL_DBCV;
import static ccpd.es.p8config.P8Config.PConfig;
import java.util.List;
import java.util.ListIterator;
import javax.swing.table.DefaultTableModel;

/**
 *
 * @author Emilio David Diaus López 2008-2025
 */
public class Reg_IncidenceRegister extends javax.swing.JPanel {

    private static final long serialVersionUID = 8696257886213116476L;

    /**
     * Creates new form IncidenceRegister
     */
    public Reg_IncidenceRegister() {
        initComponents();
    }

    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        jScrollPane1 = new javax.swing.JScrollPane();
        JPTable = new javax.swing.JTable();

        JPTable.setModel(new javax.swing.table.DefaultTableModel(
            new Object [][] {
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null},
                {null, null, null, null}
            },
            new String [] {
                "Title 1", "Title 2", "Title 3", "Title 4"
            }
        ));
        jScrollPane1.setViewportView(JPTable);

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(this);
        this.setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGroup(layout.createSequentialGroup()
                .addContainerGap()
                .addComponent(jScrollPane1, javax.swing.GroupLayout.PREFERRED_SIZE, 375, javax.swing.GroupLayout.PREFERRED_SIZE)
                .addContainerGap(19, Short.MAX_VALUE))
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGroup(layout.createSequentialGroup()
                .addContainerGap()
                .addComponent(jScrollPane1, javax.swing.GroupLayout.PREFERRED_SIZE, 275, javax.swing.GroupLayout.PREFERRED_SIZE)
                .addContainerGap(19, Short.MAX_VALUE))
        );
    }// </editor-fold>//GEN-END:initComponents

    // Variables declaration - do not modify//GEN-BEGIN:variables
    private javax.swing.JTable JPTable;
    private javax.swing.JScrollPane jScrollPane1;
    // End of variables declaration//GEN-END:variables

    FDBManExtra DBMSAP;
    List<String> DEST;

    private void FillPanel() {
        DefaultTableModel model = new DefaultTableModel();
        model.addColumn(" idregistro ");
        model.addColumn(" NCliente ");
        model.addColumn(" NEmpresa ");
        model.addColumn(" Tipo de incidencia ");
        model.addColumn(" Informe de la incidencia ");
        model.addColumn(" Persona responsable del informe ");
        model.addColumn(" Acciones tomadas ");
        model.addColumn(" Procedimiento de recuperación ");
        model.addColumn(" Datos restaurados ");
        model.addColumn(" Datos guardados ");
        model.addColumn(" Notas ");
        model.addColumn(" Fecha ");
        model.addColumn(" Nivel de seguridad ");
        model.setRowCount(0);
        int index = 1;
        if (!DEST.isEmpty()) {
            ListIterator<String> dli = DEST.listIterator();
            while (dli.hasNext()) {
                String TheNext = " " + dli.next() + " ";
                var Number = String.format("%03d", index);
                model.addRow(new Object[]{Number, TheNext});
                index++;
            }
            JPTable.setModel(model);
        }
        AddEmptyRow();

    }

    private void LoadList() {
        //SELECT * FROM ccpd_registers.list_cat_desttransint;
        PConfig.setDBName("ccpd_registers");
        DBMSAP = new FDBManExtra(PConfig.getSQLURL(), PConfig.getAdminDBPassword(), PConfig.getDBName(), PConfig.getAdminDBName(), FDBManExtra.DB_MYSQL_Selected);
        DEST = DBMSAP.LoadList("incidenceregister", "idincidenceregister");

    }

    private void UpdateDBWithTableData() {
        int ntf = AcceptInformation("¿Está seguro de que quiere actualizar los datos?");
        if (ntf == mconfyes) {
            UpdateDBData();
        }
    }

    void AddEmptyRow() {
        int index = JPTable.getModel().getRowCount() + 1;
        DefaultTableModel model = (DefaultTableModel) JPTable.getModel();
        var Number = String.format("%03d", index);
        model.addRow(new Object[]{Number, ""});
    }

    private void InsertRow() {
        int index = JPTable.getModel().getRowCount() - 1;
        String sIndex = (String) JPTable.getModel().getValueAt(index, 0);
        String sClient = (String) JPTable.getModel().getValueAt(index, 1);
        String sEnterprise = (String) JPTable.getModel().getValueAt(index, 2);
        String sTIncidence = (String) JPTable.getModel().getValueAt(index, 3);
        String sInReport = (String) JPTable.getModel().getValueAt(index, 4);
        String sREspRep = (String) JPTable.getModel().getValueAt(index, 5);
        String sActions = (String) JPTable.getModel().getValueAt(index, 6);
        String sRecProc = (String) JPTable.getModel().getValueAt(index, 7);
        String sRestData = (String) JPTable.getModel().getValueAt(index, 8);
        String sSavedData = (String) JPTable.getModel().getValueAt(index, 9);
        String sNotes = (String) JPTable.getModel().getValueAt(index, 10);
        String sDate = (String) JPTable.getModel().getValueAt(index, 11);
        String sSecLebel = (String) JPTable.getModel().getValueAt(index, 12);
        //CL_DBCV CamposPanel=new CL_DBCV();
        CLAllObjectList<CL_DBCV> List_CamposPanel = new CLAllObjectList<>();
        List_CamposPanel.add(new CL_DBCV(sClient, sIndex));
        List_CamposPanel.add(new CL_DBCV(sEnterprise, sIndex));
        List_CamposPanel.add(new CL_DBCV(sTIncidence, sIndex));
        List_CamposPanel.add(new CL_DBCV(sInReport, sIndex));
        List_CamposPanel.add(new CL_DBCV(sREspRep, sIndex));
        List_CamposPanel.add(new CL_DBCV(sActions, sIndex));
        List_CamposPanel.add(new CL_DBCV(sRecProc, sIndex));
        List_CamposPanel.add(new CL_DBCV(sRestData, sIndex));
        List_CamposPanel.add(new CL_DBCV(sSavedData, sIndex));
        List_CamposPanel.add(new CL_DBCV(sNotes, sIndex));
        List_CamposPanel.add(new CL_DBCV(sDate, sIndex));
        List_CamposPanel.add(new CL_DBCV(sSecLebel, sIndex));

        DBMSAP.InsertDatainDB("incidenceregister", List_CamposPanel);
        AddEmptyRow();
    }

    private void DeleteRow() {
        int ntf = AcceptInformation("¿Está seguro de que quiere borrar la fila seleccionada?");
        if (ntf == mconfyes) {
            int index = JPTable.getSelectedRow();
            ((DefaultTableModel) JPTable.getModel()).removeRow(index);
        }
    }

    private void UpdateDBData() {
        int totalindex = JPTable.getModel().getRowCount();
        int index = 0;
        CLAllObjectList<CL_DBCV> List_CamposPanel = new CLAllObjectList<>();
        while (index < totalindex) {
            String sIndex = (String) JPTable.getModel().getValueAt(index, 0);
            String sClient = (String) JPTable.getModel().getValueAt(index, 1);
            String sEnterprise = (String) JPTable.getModel().getValueAt(index, 2);
            String sTIncidence = (String) JPTable.getModel().getValueAt(index, 3);
            String sInReport = (String) JPTable.getModel().getValueAt(index, 4);
            String sREspRep = (String) JPTable.getModel().getValueAt(index, 5);
            String sActions = (String) JPTable.getModel().getValueAt(index, 6);
            String sRecProc = (String) JPTable.getModel().getValueAt(index, 7);
            String sRestData = (String) JPTable.getModel().getValueAt(index, 8);
            String sSavedData = (String) JPTable.getModel().getValueAt(index, 9);
            String sNotes = (String) JPTable.getModel().getValueAt(index, 10);
            String sDate = (String) JPTable.getModel().getValueAt(index, 11);
            String sSecLebel = (String) JPTable.getModel().getValueAt(index, 12);
            if (!"".equals(sClient)) {
                List_CamposPanel.add(new CL_DBCV(sClient, sIndex));
            }
            if (!"".equals(sEnterprise)) {
                List_CamposPanel.add(new CL_DBCV(sEnterprise, sIndex));
            }
            if (!"".equals(sTIncidence)) {
                List_CamposPanel.add(new CL_DBCV(sTIncidence, sIndex));
            }
            if (!"".equals(sInReport)) {
                List_CamposPanel.add(new CL_DBCV(sInReport, sIndex));
            }
            if (!"".equals(sREspRep)) {
                List_CamposPanel.add(new CL_DBCV(sREspRep, sIndex));
            }
            if (!"".equals(sRestData)) {
                List_CamposPanel.add(new CL_DBCV(sRestData, sIndex));
            }
            if (!"".equals(sRecProc)) {
                List_CamposPanel.add(new CL_DBCV(sRecProc, sIndex));
            }
            if (!"".equals(sSavedData)) {
                List_CamposPanel.add(new CL_DBCV(sSavedData, sIndex));
            }
            if (!"".equals(sNotes)) {
                List_CamposPanel.add(new CL_DBCV(sNotes, sIndex));
            }
            if (!"".equals(sDate)) {
                List_CamposPanel.add(new CL_DBCV(sDate, sIndex));
            }
            if (!"".equals(sSecLebel)) {
                List_CamposPanel.add(new CL_DBCV(sSecLebel, sIndex));
            }
            index++;
        }
        DBMSAP.DeleteDataTable("incidenceregister");
        DBMSAP.InsertDatainDB("incidenceregister", List_CamposPanel);
    }
}
